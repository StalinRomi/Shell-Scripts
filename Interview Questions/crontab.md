Crontab automatically does the job you specify at the specified time & date.

### Usecase

If we need to report node health, everyday at 6 pm, then we can set the crontab to automatically execute the script for us at 6 pm.
